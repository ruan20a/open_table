<!DOCTYPE html>
<html>
<head>
  <title>OpenTable</title>
  <!--[if lt IE9]>
    <script src="http:html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
  <![endif]-->
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>
<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
      <span class="sr-only">Toggle Navigation </span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      </button>

      <a class="navbar-brand" href="#">Open Table</a>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class ="nav navbar-nav">
        <!-- STYLE 1 -->
        <li class="active"><%=link_to "Browse Restaurants", restaurants_path%></li>
        <!-- STYLE 2 -->
        <li><%=link_to "Add New Restaurants", new_restaurant_path%></li>
        <!-- Dropdown -->
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">Account-Related Dropdown <b class="caret"></b></a>
          <ul class="dropdown-menu">
            <li><%=link_to "ACCOUNT", restaurants_path%></li>
            <li><%=link_to "ACCOUNT", new_restaurant_path%></li>
            <li><%=link_to "ACCOUNT", restaurants_path%></li>
            <li><%=link_to "ACCOUNT", restaurants_path%></li>
          </ul>
        </li>
        <% if owner_signed_in? %>
          Logged in as <strong><%= current_owner.email %></strong>
          <%= link_to 'Edit profile', edit_owner_registration_path, class: 'navbar-link'%> |
          <%= link_to 'Logout', destroy_owner_session_path, method: :delete, class: 'navbar-link' %>
        <% else %>
          <%= link_to 'Sign up', new_owner_registration_path%> |
          <%= link_to 'Login', new_owner_session_path%>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
<div class="container">
  <%= yield %>
</div>
</body>
</html>
