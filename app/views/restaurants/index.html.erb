<h1>Restaurants</h1>

<ul>
   <% @restaurants.each do |restaurant| %>
    <li>
      <%= link_to restaurant.name, restaurant_path(id: restaurant.id)%>
    </li>
    <li>
      <%= restaurant.owner%>
    </li>
  <% end %>
</ul>

